# Approve esbuild to run its postinstall (validates the native binary).
# pnpm 12 gates dependency build scripts and errors on unapproved ones
# (ERR_PNPM_IGNORED_BUILDS); pnpm manages this file via "pnpm approve-builds".
allowBuilds:
  esbuild: true
